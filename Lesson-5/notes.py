# A heap is a tree-based data structure in which all the nodes of the tree are in a specific order.
# Max heap, parent as an higher value than its child(s) / min heap: parent have lower value
# https://he-s3.s3.amazonaws.com/media/uploads/2270b0f.jpg

# Heapify, insert a element as leaf and sort the tree until this value reach the right place, worst case O(log(n))